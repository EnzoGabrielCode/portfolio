<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Projeto</title>
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
    <header>
        <%- include('partials/navbar') %>
    </header>

    <main class="admin-container">

        <h2>Editando: <%= projeto.titulo %></h2>
        <form class="admin-form" action="/projetos/edit/<%= projeto.titulo %>" method="POST">
            <div>
                <label for="novoTitulo">Novo Título:</label>
                <input type="text" id="novoTitulo" name="novoTitulo" value="<%= projeto.titulo %>" required>
            </div>
            <div>
                <label for="novaDescricao">Nova Descrição:</label>
                <textarea id="novaDescricao" name="novaDescricao" rows="4" required><%= projeto.descricao %></textarea>
            </div>
            <div>
                <label for="novoLink">Novo Link do Projeto:</label>
                <input type="url" id="novoLink" name="novoLink" value="<%= projeto.link %>">
            </div>
            <div>
                <label for="novoGitHub">Novo Link do GitHub:</label>
                <input type="url" id="novoGitHub" name="novoGitHub" value="<%= projeto.gitHub %>">
            </div>
            <div>
                <label for="novoPng">Novo Nome da Imagem:</label>
                <input type="text" id="novoPng" name="novoPng" value="<%= projeto.png %>">
            </div>
            <button type="submit" class="btn up" style="width: auto; padding: 10px 30px; margin-top: 10px;">Salvar Alterações</button>
        </form>
        
        <a href="/projetos/crud" class="link-voltar">Cancelar e Voltar</a>

    </main>

    <%- include('partials/footer') %>
</body>
</html>